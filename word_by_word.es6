import dom from 'dom';
import article from 'article';

article.ready.then(() => {

  console.log("opening word-by-word");
/*
  var splitText = function(text) {
    return text.split(" ");
  }


  var texts = document.getElementsByClassName("word-by-word");
  for (var i = 0; i<texts.length; i++) {
    console.log(splitText(texts[i]));

  }
*/
});
